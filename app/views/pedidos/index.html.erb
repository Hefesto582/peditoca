<p id="notice"><%= notice %></p>


  <% @pedidos.each do |pedido| %>
  <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"><%= pedido.codigo %> - Mesa <%= pedido.mesa %></span>
          <p><%= pedido.descricao %></p>
        </div>
        <div class="card-action">
          <%= link_to 'Concluido', pedido, method: :delete %>
           <td><%= link_to 'Editar', edit_pedido_path(pedido) %></td>
        </div>
      </div>
    </div>
    </div>
  <% end %>
  
</table>

<br>

<%= link_to 'New Pedido', new_pedido_path,  {:class => 'btn waves-effect waves-light submit-color'} %>