<%= form_with(model: prato, local: true) do |form| %>
  <% if prato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prato.errors.count, "error") %> prohibited this prato from being saved:</h2>

      <ul>
        <% prato.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.number_field :codigo %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :ingredientes %>
    <%= form.text_field :ingredientes %>
  </div>

  <div class="actions btn waves-effect waves-light submit-color">
    <i class="material-icons right">send</i>
    <%= form.submit %>
  </div>
<% end %>
